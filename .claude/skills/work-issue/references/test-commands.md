# Test Commands

Oricade is a single static-site project — one test suite, no backend/web/mobile split.

**Run targeted tests:**
```bash
npm test -- -t "<test name>"
```

**Run full suite:**
```bash
npm test
```

> These commands assume the npm scaffold from issue 01 has landed. Until then, there is no test suite to run.
